import React from "react";

// Stub: the live mikro image preview has not been ported yet.
export const LatestImage = (_props: Record<string, unknown>) => {
  return (
    <div className="my-4 rounded-md border border-fd-border bg-fd-card p-4 text-sm text-fd-muted-foreground">
      Live image preview is not available in this documentation build.
    </div>
  );
};

export default LatestImage;
